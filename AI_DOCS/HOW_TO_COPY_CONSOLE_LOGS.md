# 📋 Як скопіювати логи з консолі браузера

## Для користувача

### Chrome/Edge DevTools:

1. **Відкрити консоль:**
   - Натисніть `F12` або `Cmd+Option+I` (Mac) / `Ctrl+Shift+I` (Windows)
   - Вкладка **"Console"**

2. **Скопіювати логи:**
   - **Всі логи:** Клікніть правою кнопкою на консоль → **"Save as..."** або виділіть все (`Cmd+A` / `Ctrl+A`) → `Cmd+C` / `Ctrl+C`
   - **Конкретні логи:** Виділіть потрібні рядки → `Cmd+C` / `Ctrl+C`
   - **Експорт:** Клікніть правою кнопкою → **"Save as..."** → збережіть як `.txt` файл

3. **Фільтрація:**
   - Введіть текст у поле фільтра (наприклад, `wallet`, `TON`, `error`)
   - Скопіюйте відфільтровані логи

### Firefox DevTools:

1. Відкрити: `F12` або `Cmd+Option+K` (Mac) / `Ctrl+Shift+K` (Windows)
2. Вкладка **"Console"**
3. Скопіювати так само, як у Chrome

## Для AI (автоматичний перегляд)

### Через API endpoint:

```bash
# Останні логи з сервера
curl -k "https://api-production-57e8.up.railway.app/api/v1/admin/logs?limit=50"

# Тільки помилки
curl -k "https://api-production-57e8.up.railway.app/api/v1/admin/logs?level=ERROR"

# Пошук по тексту
curl -k "https://api-production-57e8.up.railway.app/api/v1/admin/logs?search=wallet"
```

### Через Railway Dashboard:

1. Відкрити: https://railway.app
2. Проект → **"Deployments"** → останній deployment → **"View Logs"**
3. Скопіювати логи з веб-інтерфейсу

## Що шукати в логах:

- `ERROR` - помилки
- `Auto-fetched avatar` - успішне отримання іконок
- `Could not auto-fetch avatar` - проблеми з отриманням іконок
- `TON Connect` - проблеми з wallet
- `Polling timeout` - wallet не підключається
