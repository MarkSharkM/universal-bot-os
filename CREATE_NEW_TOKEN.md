# 🔑 Створення нового GitHub токену

## Кроки:

1. **Відкрий:** https://github.com/settings/tokens
2. **Натисни:** "Generate new token" → "Generate new token (classic)"
3. **Налаштування:**
   - Note: `Universal Bot OS`
   - Expiration: `90 days` (або більше)
   - Scopes: відміть `repo` (повний доступ до репозиторіїв)
4. **Натисни:** "Generate token"
5. **Скопіюй токен** (показується тільки один раз!)

---

## Після створення токену:

```bash
cd ~/Desktop/mark/railway-mcp-project/universal-bot-os

# Push з новим токеном
git push
# Username: MarkSharkM
# Password: [встав новий токен]
```

---

## Альтернатива: GitHub CLI (простіше)

```bash
# Встанови GitHub CLI
brew install gh

# Авторизуйся (відкриє браузер)
gh auth login

# Push
cd ~/Desktop/mark/railway-mcp-project/universal-bot-os
git push
```

GitHub CLI автоматично використає правильну автентифікацію!

---

**Рекомендація:** Використай GitHub CLI - це найпростіше! 🚀

