# 🔑 Створення нового GitHub токену

## 📋 Кроки:

### 1. Відкрий сторінку токенів:
https://github.com/settings/tokens

### 2. Натисни "Generate new token":
- Виберіть **"Generate new token (classic)"** (не fine-grained)

### 3. Налаштування токену:
- **Note:** `Universal Bot OS` (будь-яка назва)
- **Expiration:** `90 days` (або більше, навіть "No expiration")
- **Scopes:** Відміть:
  - ✅ `repo` (повний доступ до репозиторіїв)
    - Це дасть доступ до всіх репозиторіїв

### 4. Натисни "Generate token"

### 5. Скопіюй токен:
- ⚠️ **Важливо:** Токен показується тільки один раз!
- Скопіюй його кудись безпечно

---

## ✅ Після створення токену:

**Додай токен в .env:**
```bash
cd ~/Desktop/mark/railway-mcp-project
# Відкрий .env і додай:
GITHUB_PAT=новий-токен-тут
```

**Або використай для push:**
```bash
cd ~/Desktop/mark/railway-mcp-project/universal-bot-os
git push
# Username: MarkSharkM
# Password: [встав новий токен]
```

---

## 🔐 Альтернатива: SSH ключ (безпечніше)

Якщо хочеш використати SSH:

1. **Створи SSH ключ:**
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   # Натисни Enter для всіх питань
   ```

2. **Скопіюй публічний ключ:**
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```

3. **Додай в GitHub:**
   - Відкрий: https://github.com/settings/keys
   - Натисни "New SSH key"
   - Встав скопійований ключ
   - Натисни "Add SSH key"

4. **Зміни remote на SSH:**
   ```bash
   cd ~/Desktop/mark/railway-mcp-project/universal-bot-os
   git remote set-url origin git@github.com:MarkSharkM/universal-bot-os.git
   git push
   ```

---

**Рекомендація:** Створи новий токен з правами `repo` - це найпростіше! 🚀

