# ✅ Deployment Checklist - Universal Bot OS

## 📋 Перед деплоєм

### 1. Перевірка коду
- [x] ✅ Dockerfile створено
- [x] ✅ railway.json налаштовано
- [x] ✅ requirements.txt оновлено
- [x] ✅ Всі моделі імпортовані в main.py
- [x] ✅ Таблиці створюються автоматично при старті

### 2. Railway налаштування
- [x] ✅ Проект створено: `universal-bot-os`
- [x] ✅ Сервіс `api` створено
- [x] ✅ PostgreSQL додано
- [x] ✅ Redis додано
- [x] ✅ Змінні оточення додані:
  - [x] `DATABASE_URL`
  - [x] `REDIS_URL`
  - [x] `SECRET_KEY`
  - [x] `ANTHROPIC_API_KEY`

---

## 🚀 Деплой

### Варіант 1: Через GitHub (рекомендовано)

1. **Завантаж код в GitHub:**
   ```bash
   cd universal-bot-os
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <your-github-repo-url>
   git push -u origin main
   ```

2. **Підключи GitHub до Railway:**
   - Відкрий Railway проект
   - Settings → Source → Connect GitHub
   - Виберіть репозиторій
   - Railway автоматично задеплоїть

### Варіант 2: Через Railway CLI

```bash
cd universal-bot-os
railway login
railway link  # вибери проект universal-bot-os
railway up
```

### Варіант 3: Через Railway UI

1. Відкрий проект на Railway
2. Сервіс `api` → Settings → Source
3. Підключи GitHub або завантаж код

---

## ✅ Після деплою

### 1. Перевірка таблиць БД

Таблиці створюються автоматично при старті додатку. Перевір:

```bash
# Через Railway CLI
railway run python scripts/create_tables.py

# Або через Railway UI → Logs
# Має бути: "✅ Database tables created/verified"
```

### 2. Перевірка health check

```bash
# Отримай URL з Railway
curl https://your-app.railway.app/health
```

Очікуваний результат:
```json
{
  "status": "healthy",
  "database": "connected",
  "redis": "connected"
}
```

### 3. Перевірка Admin UI

Відкрий: `https://your-app.railway.app/admin`

### 4. Перевірка API docs

Відкрий: `https://your-app.railway.app/docs`

---

## 🤖 Підключення Telegram бота

### 1. Створи бота через BotFather
- Отримай токен

### 2. Додай бота через Admin UI
- Відкрий `/admin`
- Вкладка "Bots" → "Create Bot"
- Введи токен та налаштування

### 3. Налаштуй webhook

```bash
# Отримай Railway URL з налаштувань сервісу
RAILWAY_URL="https://your-app.railway.app"
BOT_TOKEN="your-bot-token"

curl -X POST "https://api.telegram.org/bot${BOT_TOKEN}/setWebhook" \
  -d "url=${RAILWAY_URL}/api/v1/webhooks/telegram/${BOT_TOKEN}"
```

---

## 🔍 Перевірка логів

```bash
# Через Railway CLI
railway logs

# Або через Railway UI → Logs
```

**Що шукати:**
- ✅ "🚀 Universal Bot OS starting up..."
- ✅ "✅ Database tables created/verified"
- ✅ "Application startup complete"
- ❌ Помилки підключення до БД
- ❌ Помилки імпорту модулів

---

## 🐛 Troubleshooting

### Помилка: "Table does not exist"
**Рішення:** Таблиці не створилися. Перевір логи при старті.

### Помилка: "Connection refused" (БД)
**Рішення:** Перевір `DATABASE_URL` в змінних оточення.

### Помилка: "Module not found"
**Рішення:** Перевір `requirements.txt` та перезапусти деплой.

### Сервіс не запускається
**Рішення:**
1. Перевір логи
2. Перевір змінні оточення
3. Перевір Dockerfile

---

## 📊 Статус

**Поточний стан:**
- ✅ Railway проект налаштовано
- ✅ Бази даних додані
- ✅ Змінні оточення встановлені
- ⏳ Очікується деплой коду

**Наступний крок:** Задеплоїти код через GitHub або Railway CLI.

---

**Готово до деплою!** 🚀

