# 📤 Як задеплоїти виправлення

## Проблема:
Git push не працює автоматично через автентифікацію.

## Рішення:

### Варіант 1: Push вручну (найпростіше)

```bash
cd universal-bot-os
git push
```

Якщо попросить пароль - введи GitHub токен або налаштуй SSH ключ.

### Варіант 2: Через GitHub Desktop

1. Відкрий GitHub Desktop
2. Відкрий репозиторій `universal-bot-os`
3. Натисни "Push origin"

### Варіант 3: Через Railway UI (якщо webhook налаштовано)

Railway може автоматично підхопити зміни, якщо:
- GitHub webhook налаштовано
- Railway автоматично перевіряє зміни

Просто зачекай кілька хвилин - Railway може автоматично задеплоїти.

---

## Що виправлено:

1. ✅ `railway.json` - виправлено PORT змінну
2. ✅ `Dockerfile` - виправлено PORT змінну
3. ✅ `app/main.py` - виправлено Admin UI роутинг

**Після деплою:**
- Помилка з PORT має зникнути
- Додаток має запуститися
- `/admin` має працювати

---

**Найпростіше:** Зроби `git push` вручну в терміналі.

