# 📤 Ручний push в GitHub

Git push не працює автоматично через автентифікацію. Зроби push вручну:

```bash
cd universal-bot-os
git push
```

Або якщо потрібен токен:

```bash
git push https://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@github.com/MarkSharkM/universal-bot-os.git main
```

**Альтернатива:** Railway може автоматично підхопити зміни, якщо GitHub webhook налаштовано.

---

**Після push:** Railway автоматично задеплоїть виправлення для Admin UI.

