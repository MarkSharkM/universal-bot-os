# 🌐 Налаштування Public Domain в Railway

## ✅ Статус: НАЛАШТОВАНО

**Домен:** `https://api-production-57e8.up.railway.app`

**Перевірено:**
- ✅ `/` - працює (повертає JSON з інформацією про API)
- ✅ `/admin` - працює (показує Admin UI)
- ✅ `/health` - працює (health check)
- ✅ `/api/v1/admin/bots` - готовий до використання

---

## ❌ Проблема (вирішена)
Всі endpoints (`/`, `/admin`, `/test-admin`) повертали 404, а на сторінці показувалося "Home of the Railway API" замість нашого FastAPI додатку.

**Причина:** Railway не знав, який сервіс обробляти публічні запити, тому що public domain не був налаштований.

---

## ✅ Рішення: Налаштувати Public Domain

### Крок 1: Відкрий налаштування сервісу `api`

1. Зайди в Railway Dashboard
2. Відкрий проект `universal-bot-os`
3. Натисни на сервіс **`api`** (той, що з GitHub Octocat іконкою)

### Крок 2: Перейди в Settings → Networking

1. В сервісі `api` натисни вкладку **"Settings"**
2. Знайди розділ **"Networking"** або **"Domains"**

### Крок 3: Створи Public Domain

1. Натисни кнопку **"Generate Domain"** або **"Add Domain"**
2. Railway автоматично згенерує домен типу:
   - `universal-bot-os-production-xxxx.up.railway.app`
   - або `api-production-xxxx.up.railway.app`
3. Скопіюй згенерований URL

### Крок 4: Перевір роботу

Відкрий в браузері згенерований URL:
- `https://api-production-57e8.up.railway.app/` - ✅ працює (показує JSON з інформацією про API)
- `https://api-production-57e8.up.railway.app/health` - ✅ працює (health status)
- `https://api-production-57e8.up.railway.app/admin` - ✅ працює (Admin UI)

---

## 🔍 Альтернативна перевірка

Якщо не бачиш кнопку "Generate Domain", спробуй:

1. **Перевір, чи сервіс має публічний доступ:**
   - В Settings → Networking має бути опція "Public" або "Expose Port"
   - Переконайся, що сервіс не є "Private"

2. **Перевір Environment Variables:**
   - Може бути змінна `RAILWAY_PUBLIC_DOMAIN` або подібна
   - Перевір, чи немає конфліктів з доменами

3. **Перевір Deploy Logs:**
   - Після налаштування домену перевір логи
   - Мають з'явитися запити з новим доменом

---

## 📝 Після налаштування

Після того, як домен налаштований:

1. **Оновіть Telegram Webhook URL:**
   ```
   https://api-production-57e8.up.railway.app/api/v1/webhooks/telegram/{bot_token}
   ```

2. **Перевірте всі endpoints:**
   - `/` - кореневий endpoint
   - `/health` - health check
   - `/admin` - Admin UI
   - `/api/v1/admin/bots` - Admin API

3. **Перевірте логи:**
   - В Deploy Logs мають з'явитися записи про запити
   - Мають з'явитися повідомлення типу `🌐 GET / - Full URL: ...`

---

## ⚠️ Важливо

- **Subscription Warning:** Якщо бачиш "Your subscription is past due", це може впливати на функціональність. Рекомендую вирішити це питання.

- **Порт:** Railway автоматично встановлює `PORT` через змінну оточення. Додаток слухає на порту, який встановлює Railway (зазвичай 8080 або інший).

- **Домен:** Після налаштування домену може знадобитися 1-2 хвилини для активації.

