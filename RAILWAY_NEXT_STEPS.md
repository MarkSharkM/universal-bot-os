# 🚂 Railway - Наступні кроки

## 📊 Поточний статус:

✅ **Проект створено:** `universal-bot-os` (ID: `46aa6dc7-1bb1-49b7-ac65-e9a8ac73636a`)
✅ **Сервіс 'api' створено:** ID: `a9598ef7-0499-439f-bf3c-c6de5f3cd022`
❌ **PostgreSQL:** Потрібно додати вручну
❌ **Redis:** Потрібно додати вручну

---

## 🔧 Що потрібно зробити вручну:

### 1. Додати PostgreSQL та Redis

**Через Railway UI:**
1. Відкрий: https://railway.app/project/46aa6dc7-1bb1-49b7-ac65-e9a8ac73636a
2. Натисни **"New"** → **"Database"** → **"PostgreSQL"**
3. Натисни **"New"** → **"Database"** → **"Redis"**
4. Railway автоматично створить змінні `DATABASE_URL` та `REDIS_URL` для сервісу `api`

**⚠️ Важливо:** Railway API не підтримує створення плагінів (PostgreSQL/Redis) через GraphQL API. Потрібно додати вручну через UI.

---

### 2. Налаштування AI через адмінку

**✅ Добре:** AI конфігурація тепер зберігається в `bot.config['ai']` через адмінку!

**Як це працює:**
- API ключі зберігаються в БД (в `bot.config['ai']['api_key']`)
- Налаштування через Admin UI: `/admin` → вкладка "AI Config"
- Fallback на змінні оточення, якщо не встановлено в адмінці

**Через Admin UI:**
1. Відкрий `/admin`
2. Вкладка "AI Config"
3. Вибери бота
4. Встанови:
   - Provider (OpenAI/Anthropic)
   - Model
   - API Key (зберігається в БД, не в env vars!)
   - Temperature
   - System Prompt

**Переваги:**
- ✅ Кожен бот може мати свій AI провайдер/ключ
- ✅ Не потрібно змінних оточення для кожного бота
- ✅ Легко змінювати через адмінку
- ✅ Безпечно (ключі в БД, не в env vars)

---

### 3. Змінні оточення (опціонально)

**Мінімальні змінні для запуску:**
- `DATABASE_URL` - автоматично з PostgreSQL
- `REDIS_URL` - автоматично з Redis
- `SECRET_KEY` - згенеруй: `python -c "import secrets; print(secrets.token_urlsafe(32))"`

**AI ключі:**
- ❌ `OPENAI_API_KEY` - **НЕ потрібно** (налаштовується через адмінку)
- ❌ `ANTHROPIC_API_KEY` - **НЕ потрібно** (налаштовується через адмінку)

**Fallback:** Якщо API ключ не встановлено в адмінці, система спробує використати змінні оточення (для сумісності).

---

## 🎯 Підсумок:

1. ✅ Проект створено
2. ✅ Сервіс 'api' створено
3. ⏳ Додай PostgreSQL та Redis через UI
4. ⏳ Встанови `SECRET_KEY` в змінних оточення
5. ✅ AI налаштування через адмінку (не потрібні env vars!)

**Railway UI:** https://railway.app/project/46aa6dc7-1bb1-49b7-ac65-e9a8ac73636a

---

**💡 Порада:** Після додавання PostgreSQL та Redis, Railway автоматично створить `DATABASE_URL` та `REDIS_URL`. Потім просто додай `SECRET_KEY` вручну, і все готово!

